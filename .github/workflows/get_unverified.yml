name: Get Unverified
on:
  workflow_dispatch
jobs:
  getUnverified:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3
         with:
          fetch-depth: 0
       - run: |
          shopt -s globstar extglob
          python3 checks/get_unverified.py docs/**/*.md
